@model IEnumerable<GradeDto>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_TeachersLayout.cshtml";
}


<h4 style="text-align:justify">Grades</h4>

<table class="table table-hover table-dark" aria-labelledby="UniversityApp">
    <thead>
        <tr>
            <th scope="col">
                @Html.DisplayNameFor(model => model.Value)
            </th>
            <th scope="col">
                @Html.DisplayNameFor(model => model.Date)
            </th>
            <th scope="col">
                @Html.DisplayNameFor(model => model.Enrollment)
            </th>
            <th scope="col"></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Value)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Date)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Enrollment.EnrollmentId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.GradeId">Edit</a> |
                <a asp-action="Delete" asp-route-id="@item.GradeId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
