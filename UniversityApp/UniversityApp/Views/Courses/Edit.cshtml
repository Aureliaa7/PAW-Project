@model CourseDto;

@{
    ViewData["Title"] = "Edit";
    Layout = "~/Views/Shared/_SecretariesLayout.cshtml";
}

@*Send Model because we want the form to contain the course's details*@

@{await Html.RenderPartialAsync(
    "_CoursePartial", 
    Model,
    new ViewDataDictionary(this.ViewData){
        {"ActionName", "Edit"},
        {"ButtonText", "Save"},
        {"FormName", "Edit course"}
    });
}