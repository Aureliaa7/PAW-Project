@model IEnumerable<UniversityApp.Core.DomainEntities.TeachedCourse>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_SecretariesLayout.cshtml";
}

<table class="table" aria-labelledby="UniversityApp">
    <thead>
        <tr>
            <th scope="col">
                @Html.DisplayNameFor(model => model.Course)
            </th>
            <th scope="col">
                @Html.DisplayNameFor(model => model.Teacher)
            </th>
            <th scope="col"></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Course.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Teacher.Id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
